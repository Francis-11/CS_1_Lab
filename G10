class Counter:
    counter = 0
    def increment(self):
        #increment method used to increment value of counter variable
        self.counter = self.counter + 1
    def get_value(self):
        #get_value method used to return value of variable counter
        return self.counter
